# TODO (Athena Project)

- [x] Create SQL bootstrap file for database `athena_1`.
- [x] Update backend MySQL connection to use `athena_1` (api/config.php).
- [ ] (After changes) Run `db/athena_1_create.sql` in phpMyAdmin / MySQL.
- [ ] (After DB is created) Start the app and verify API endpoints connect successfully.

